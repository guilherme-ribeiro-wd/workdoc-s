import React from 'react';
import { Navbar, NavbarHeader } from 'react-bootstrap';

export default class NavHeader extends React.Component {
    render() {
        return (
            <div></div>
        );
    }
}